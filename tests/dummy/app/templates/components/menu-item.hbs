{{! template-lint-disable no-implicit-this }}
<PaperItem @onClick={{this.onClick}} @href={{this.href}} @class={{concat "menu-item" (if @active " active") (if @expanded " expanded")}}>
  {{yield}}
  {{#if this.canExpand}}
    {{paper-icon "expand-more" class="expand-icon"}}
  {{/if}}
</PaperItem>